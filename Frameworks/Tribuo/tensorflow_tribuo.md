# TensorFlow 教程

## 简介

下面介绍如何使用 Tribuo 的 TensorFlow 接口在 Tribuo 中构建深度学习模型。Tribuo 使用由 TensorFlow SIG-JVM 团队构建的 TensorFlow-Java。Tribuo 的开发团队也积极参与 SIG-JVM，努力让 TensorFlow 在 Java 平台上所有人都能运行。

> [!NOTE]
>
> 不保证 Tribuo TensorFlow 接口的稳定性。

下面训练 MLP（多层感知机）用于分类和回归，同时训练卷积神经网络（CNN）用于 MNIST 分类。